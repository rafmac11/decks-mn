# ── CRM Integration ───────────────────
# Webhook URL — receives POST with JSON lead payload
WEBHOOK_URL=https://your-crm.com/webhook/leads

# API URL — alternative/additional endpoint (uses Bearer auth)
API_URL=
API_KEY=

# ── Server ────────────────────────────
PORT=3000

# ── CORS (comma-separated origins, or leave for "*") ──
ALLOWED_ORIGINS=https://yourdomain.com
